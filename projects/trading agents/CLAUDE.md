# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Trading agents — this project is currently being initialized. Update this file once the architecture and tooling are established.

## Environment

- Python managed via `pyenv` (`~/.pyenv`)
- Node.js managed via `nvm` (`~/.nvm`)
- Homebrew at `~/homebrew/bin` (non-standard prefix)
